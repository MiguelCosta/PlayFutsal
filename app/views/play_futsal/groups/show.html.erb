<%= content_for :title do @group.name end %>

<div class="hero-unit">
  <%= render :partial => 'group', :locals => { :group => @group } %>

  <div class='actions'>
    <%= link_to 'Edit', edit_group_path(@group) %>
    <%= link_to 'Delete', group_path(@group), :method => :delete %>
  </div>
</div>