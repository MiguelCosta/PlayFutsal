<table class="table">
  <thead>
    <tr>
      <th>Team</th>
      <th>Matches Played</th>
      <th>Goals</th>
    </tr>
  </thead>

  <tbody>
    <% group.teams.each do |team| %>
      <tr>
        <td><%= link_to team.name, team_path(team) %></td>
        <td><%= team.matches.in_group.finished.count %></td>
        <td><%= link_to 'Edit', edit_team_path(team) %></td>
      </tr>
    <% end %>
  </tbody>
</table>rails 