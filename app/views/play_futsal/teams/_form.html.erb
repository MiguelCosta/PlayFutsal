<%= form_for @team do |f| %>
  <% if @team.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(@team.errors.count, 'error') %> prohibited this post from being saved:</h2>

      <ul>
      <% @team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='field'>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <!-- <div class='field'>
    <%= f.label :athletes %>
    <%= f.collection_select :athletes, PlayAuth::User.all, :id, :name, :selected => @team.athletes %>
  </div>
 -->

  <div class='field'>
    <%= f.label :athletes %>
    <%= select_tag 'athlete_ids[]', 
        options_for_select(@athletes.collect { |athl| [athl.name, athl.user.id] } , @team.athlete_ids), 
        { :multiple => true, :size =>5, :id => "athletes" } %>
  </div>

  <div class='form-actions'>
    <%= f.submit 'Save' %>
    <%= link_to  'Back', :back %>
  </div>

<% end %>