<% content_for :title do 'Teams' end %>

<div class="hero-unit">
  <%= render :partial => 'play_futsal/teams/table' %>

  <div class="actions">
    <%= link_to 'New Team', new_team_path, :class => 'btn' %>
  </div>
</div>
