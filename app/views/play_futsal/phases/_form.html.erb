<%= form_for @phase do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :position %>
  <%= f.number_field :position %>

  <%= f.submit "Save", :class => 'btn btn-primary' %>
  <%= link_to 'Back', :back, :class => :btn %>
<% end %>