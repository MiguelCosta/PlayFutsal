<%= content_for :title do 'Athletes' end %>

<div class="hero-unit">
  <%= render :partial => 'play_futsal/athletes/table', :locals => { :athletes => @athletes } %>

  <%#= link_to 'New Athlete', new_athlete_path, :class => :btn %>
</div>